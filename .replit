language = "python3"
run = "python main.py"

[nix]
channel = "stable-22_11"

[deployment]
run = ["sh", "-c", "pip install -r requirements.txt && gunicorn --bind 0.0.0.0:5000 web_server:app"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 5000
externalPort = 80
